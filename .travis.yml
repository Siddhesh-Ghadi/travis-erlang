dist: bionic
  
arch: ppc64le
  
#otp_release:
#  - 21.2
#  - 22.0
jobs:
  include:
    - env: TARGET=22.0
    - env: TARGET=21.2

script:
  - travis_wait 60 sleep 60m &
  - kerl build $TARGET
  - kerl install $TARGET $HOME/kerl/22.0
  - . $HOME/kerl/$TARGET/activate
  - erl -version
